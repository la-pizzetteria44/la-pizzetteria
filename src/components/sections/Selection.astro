---
import "../../styles/sections/selection.css";

const { pizzas, platters, specialties, drinks, desserts, suggestions } = Astro.props;
console.log(suggestions);
---
 
 <!-- SELECTION  -->
     <section class="selection">
        <h2>Nos favoris</h2>
        <p>Les incontournables de La Pizzetteria</p>

        <div class="selection-list">
            {
                suggestions.map((item: { image: string | null | undefined; name: unknown; price: string | number }) => (
                    <article class="card selection__card">
                        <img src={item.image} alt={item.name} />
                        <h3>{item.name}</h3>
                    </article>
                ))
            }
          
        </div>
    
        <div class="cta selection__cta">
            <a href="/menu" class="btn btn--secondary">
                <span>Voir plus</span>
            </a>
        </div>
     </section>
