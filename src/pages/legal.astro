---
import { Phone, Map } from "@lucide/astro";

import Layout from '../layouts/Layout.astro';
import Home from '../components/Home.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import hours from '../content/hours/hours.json';

function formatDay(day: { closed: any; slots?: any[]; }) {
  if (!day || day.closed) return "fermé";

  if (!day.slots || day.slots.length === 0) return "fermé";

  return day.slots
    .map((slot: { open: { hour: any; minute: any; }; close: { hour: any; minute: any; }; }) =>
      `${slot.open.hour}:${slot.open.minute}-${slot.close.hour}:${slot.close.minute}`
    )
    .join(" / ");
}

const daysOrder = [
  "lundi","mardi","mercredi","jeudi","vendredi","samedi","dimanche"
];

const formattedDays = daysOrder.map(dayKey => {
  const day = hours[dayKey as keyof typeof hours] ?? { closed: true };

  return {
    day: dayKey,
    value: formatDay(day)
  };
});

---

<Layout title="Mentions légales">

    <Header />

<main class="policy-legal">
      <h1>Mentions légales</h1>

      <section>
        <h2>Éditeur du site</h2>
        <p>
          Le présent site est édité par :
          <br /><strong>[Nom de l’établissement]</strong><br />
          [Adresse complète]<br />
          📞 [Téléphone]<br />
          📧 [Email]
        </p>
      </section>

      <section>
        <h2>Directeur de la publication</h2>
        <p>[Nom du gérant]</p>
      </section>

      <section>
        <h2>Hébergement</h2>
        <p>Le site est hébergé par :<br />
          [Nom hébergeur]<br />
          [Adresse]<br />
          <a href="#">Site web</a>
        </p>
      </section>

      <section>
        <h2>Propriété intellectuelle</h2>
        <p>
          L’ensemble du contenu du site (textes, images, graphismes, logo, etc.) est la propriété exclusive de [Nom de l’établissement], sauf mention contraire. Toute reproduction, distribution, modification, adaptation, retransmission ou publication de ce contenu est strictement interdite sans l’accord écrit préalable de [Nom de l’établissement].
        </p>
      </section>

      <section>
        <h2>Responsabilité</h2>
        <p>
          Les informations présentes sur le site sont fournies à titre indicatif.
L’éditeur ne saurait être tenu responsable des omissions, inexactitudes ou carences dans la mise à jour.
        </p>
      </section>

      <section>
        <h2>Crédits</h2>
        <p>
          Conception et développement du site : Romain Fournier
        </p>
      </section>
  </main>
<Footer formattedDays={formattedDays} />

</Layout>
 