---
import Nav from '../components/MenuNav.astro';
import Starters from '../components/sections/Starters.astro';
import Pizzas from '../components/sections/Pizzas.astro';
import Desserts from '../components/sections/Desserts.astro';
import Drinks from '../components/sections/Drinks.astro';

const starters = Object.values(import.meta.glob('../content/starters/*.json', { eager: true })).map(p => p.default);
const pizzas = Object.values(import.meta.glob('../content/pizzas/*.json', { eager: true })).map(p => p.default);
const drinks = Object.values(import.meta.glob('../content/drinks/*.json', { eager: true })).map(p => p.default);
const desserts = Object.values(import.meta.glob('../content/desserts/*.json', { eager: true })).map(p => p.default);
---

<main>
    <!-- STICKY MENU-->
	<Nav />

	<!-- ENTRÉES -->
    <Starters starters={starters} />

	<hr />

	<!-- PIZZAS -->
	<Pizzas pizzas={pizzas} />

	<hr />

    <!-- DESSERTS -->
    <Desserts desserts={desserts} />

	<hr />
    
    <!-- BOISSONS -->
    <Drinks drinks={drinks} /> 
</main>